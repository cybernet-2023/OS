#include "stdio.h"

void g_kmain(void)
{
    clearScreen();
    puts("Welcome to StormOS!!!");
    putc('\n');
    puts("It's new OS in console, NASM + C!");
    putc('\n');
    puts("99 percents of code in clear C!");
    putc('\n');
}